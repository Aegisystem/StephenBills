/**
 * Enum que representa los tipos de documentos de identidad según la DIAN
 */
export enum DocumentType {
  NIT = 'NIT',
  CC = 'CC',
  CE = 'CE',
  PASSPORT = 'PASSPORT',
  FOREIGN_ID = 'FOREIGN_ID'
} 